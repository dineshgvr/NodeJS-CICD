pipeline {
    agent any
    tools {
            nodejs "Node22"   // 👈 same name you gave in Global Tool Configuration
        }
        stages {
            stage("checkout") {
            steps {
                checkout scm
                   }
            }

                  stage("test") {
                        steps {
                           sh 'npm install'
                           sh 'npm test'
                        }
                        }


          stage("build") {
                steps {
                    sh 'npm run build'
                }
                }
    }
}
